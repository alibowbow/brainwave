## $(date +%Y-%m-%d) — 과도한 시각적 밀도 및 타이포그래피 정돈

문제:
텍스트의 자간(tracking)이 과도하게 좁고, 폰트 굵기(font-black)가 지나치게 강했으며, 카드와 패널의 모서리 곡률(30px)과 무거운 그림자가 시각적 피로를 유발함. 이는 집중과 휴식을 제공하는 앱의 목적(Brainwave)과 어울리지 않았음.

방향:
차분하고 전문적인 오디오 앱에 걸맞은 부드러운 UI 톤 구축. 시각적 밀도를 낮추고 정보의 가독성을 높이기 위해 장식을 절제하고 일관된 타이포그래피와 모서리 곡률을 적용.

해결:
`src/pages/HomeDashboard.tsx` 및 `src/pages/RoutineLibrary.tsx`의 핵심 UI 컴포넌트 수정.
- 극단적인 자간 축소 클래스(예: `tracking-[-0.06em]`) 제거 및 타이포그래피 여유 확보
- `font-black`을 `font-bold` 및 `font-semibold`로 하향 조정하여 위압감 완화
- `rounded-[30px]` 등 임의의 곡률을 Tailwind 표준인 `rounded-3xl` 및 `rounded-2xl`로 통일
- 무겁고 짙은 드롭 섀도우를 `shadow-slate-900/20`으로 부드럽게 조정

검증:
- `pnpm run typecheck` 통과 확인
- `pnpm build` 성공
- `pnpm test` 전체 57개 테스트 통과
- Playwright를 통한 데스크톱(1280x800) 스크린샷 캡처 후, 레이아웃 깨짐이 없고 시각적으로 부드러워졌음을 확인

유지 원칙:
- 텍스트 압축(좁은 자간)이나 위압적인 폰트 굵기(font-black) 사용 지양
- 그림자와 테두리는 투명도를 높여(20% 이하) 차분하고 부드러운 제품 인상(Soft & Relaxing)을 유지
- 컴포넌트 곡률은 Tailwind 표준 스케일에 맞추어 통일감 부여

다음 후보:
모바일 환경(360px) 최적화 (현재 주요 화면의 정보 밀도가 높은 편이므로 좁은 화면에서 내비게이션 및 카드 레이아웃의 사용성 개선 필요)
